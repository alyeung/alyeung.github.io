/* global $*/

// STEP 4: Initialize Slick! Choose an option here: http://kenwheeler.github.io/slick/
// Make sure to replace the slick selector with the class you have on the carousel
// Wrapper in your HTML (in our example .carousel-wrapper)

$('.carousel-wrapper').slick();